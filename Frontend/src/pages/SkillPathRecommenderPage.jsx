import React from 'react';
import SkillPathRecommender from '../components/SkillPathRecommender';

const SkillPathRecommenderPage = ({ user }) => {
  return <SkillPathRecommender user={user} />;
};

export default SkillPathRecommenderPage;
